[package]
name = "abi-encode"
version = "0.1.0"
edition = "2021"
description = "Static ABI encoding with no dependencies"
license = "MIT"
repository = "https://github.com/Marco-415/abi-encode"
keywords = ["ethereum", "evm", "abi"]
categories = ["cryptography::cryptocurrencies"]

[dependencies]
